//Hourly_Graph
export type TableHeadTitleType = "pop" | "pcp" | "sno" | "reh" | "windy";

export type TableHeadDataValueType = {
  title: string;
  unit: string;
};

export type SummaryType = "current" | "tomorrow";
